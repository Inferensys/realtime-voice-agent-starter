# ElevenLabs Agent Bridge

Bridge ElevenLabs realtime conversation events into the kit's normalized event model.

## Run

```bash
cp examples/elevenlabs-agent-bridge/.env.example examples/elevenlabs-agent-bridge/.env
npm run dev
```

Use:

```text
POST /api/webhooks/elevenlabs
```
