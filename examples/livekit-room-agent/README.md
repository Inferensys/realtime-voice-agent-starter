# LiveKit Room Agent

Room-participant voice agent path for LiveKit.

## Run

```bash
cp examples/livekit-room-agent/.env.example examples/livekit-room-agent/.env
npm run dev
```

Use this when the agent joins a LiveKit room and emits transcript, tool, handoff, and latency events into the control plane.
